import React from 'react'

import Wrapper from './Wrapper'

function Header({ }) {
    return <div class="bg-gray-400">
        <Wrapper>
            <nav>Home</nav>
        </Wrapper>
    </div>
}

export default Header;